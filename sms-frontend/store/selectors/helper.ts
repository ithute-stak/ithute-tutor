/*
import {RootState} from "@/store/store";
import {UserRole} from "@/types/user";

export const isSuperAdmin = (state: RootState) =>
    state.auth.user?.role === UserRole.super_admin

export const isSchoolAdmin = (state: RootState) =>
    state.auth.user?.role === UserRole.school_admin

export const isTeacher = (state: RootState) =>
    state.auth.user?.role === UserRole.teacher

export const isStudent = (state: RootState) =>
    state.auth.user?.role === UserRole.student

export const isParent = (state: RootState) =>
    state.auth.user?.role === UserRole.parent*/
